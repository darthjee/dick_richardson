setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile.production
  config:
    RAILS_ENV: production
release:
  command:
    - rake db:migrate
  image: web
run:
  web: bundle exec rails s -p 0.0.0.0:80
