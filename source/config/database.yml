<%
  socket = [
    ENV["BOXEN_MYSQL_SOCKET"],
    "/var/run/mysql5/mysqld.sock",
    "/tmp/mysql.sock"
  ].detect { |f| f && File.exist?(f) }
  port = 5432
%>

development: &development
  adapter: postgresql
  encoding: utf8
  database: urna_development
  pool: 5
  username: <%= ENV["URNA_POSTGRES_USER"] %>
  password: <%= ENV["URNA_POSTGRES_PASSWORD"] %>
  host: <%= ENV["URNA_POSTGRES_HOST"] %>
<% if socket %>
  socket: <%= socket %>
<% else %>
  port: <%= ENV["URNA_POSTGRES_PORT"] %>
<% end %>

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &test
  <<: *development
  database: urna_test

test_readonly:
  <<: *test

production: &production
  <<: *development
  database: urna_production
